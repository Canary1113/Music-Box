<Page
    x:Class="音乐魔盒.RecognizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{x:Null}">

    <Grid Background="{x:Null}" Padding="12">
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Enabled"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Disabled">
            <StackPanel Spacing="12" MaxWidth="920">
                <TextBlock x:Name="AudioSectionTitleText" Text="音频识别 -> 五线谱（MP3/WAV）" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="AudioSectionDescText"
                           Text="支持 MP3 / WAV 离线识别：提取主旋律音高并生成草稿音符，可直接导入编辑页生成五线谱。"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock x:Name="FrequencyLabelText" Text="频率 (Hz):" VerticalAlignment="Center"/>
                    <TextBox Width="140" Text="{Binding InputFrequency, Mode=TwoWay}"/>
                    <TextBlock x:Name="FrequencyResultLabelText" Text="换算:" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding FrequencyResult}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <StackPanel Spacing="10">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="SelectAudioButton" Content="选择音频 (MP3/WAV)" Click="SelectAudioButton_Click"/>
                            <Button x:Name="AnalyzeAudioButton" Content="分析音高并生成草稿" Click="AnalyzeAudioButton_Click"/>
                            <Button x:Name="ImportDetectedNotesButton" Content="导入到当前工程" Click="ImportDetectedNotesButton_Click"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <ProgressRing x:Name="AudioAnalyzeProgressRing" Width="18" Height="18" IsActive="False" Visibility="Collapsed"/>
                            <TextBlock x:Name="AudioAnalyzeProgressText" Text="" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </StackPanel>

                        <TextBlock x:Name="SelectedAudioPathText" Text="未选择音频文件" TextWrapping="Wrap"/>
                        <TextBlock x:Name="RecognizeSummaryText" Text="识别结果: -" TextWrapping="Wrap"/>

                        <TextBox x:Name="DetectedNotesPreviewTextBox"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="220"/>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="AudioSupportText"
                           Text="当前识别器支持 MP3 / WAV，默认按单旋律提取并量化到十六分音符网格。"
                           TextWrapping="Wrap"/>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <StackPanel Spacing="10">
                        <TextBlock x:Name="OmrSectionTitleText" Text="图片 / PDF 识别 -> 五线谱（Beta）" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="OmrSectionDescText"
                                   Text="支持 PDF、PNG、JPG、BMP、TIFF。内部通过多引擎 + 多候选评分转换成 MusicXML。"
                                   TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="SelectSheetFileButton" Content="选择图片/PDF" Click="SelectSheetFileButton_Click"/>
                            <Button x:Name="RunOmrButton" Content="识别为谱面 (MusicXML)" Click="RunOmrButton_Click"/>
                            <Button x:Name="ImportBestOmrButton" Content="导入最优候选" Click="ImportBestOmrButton_Click" IsEnabled="False"/>
                            <Button x:Name="ImportSelectedOmrButton" Content="导入选中候选" Click="ImportSelectedOmrButton_Click" IsEnabled="False"/>
                            <Button x:Name="OpenOmrArtifactsButton" Content="查看中间图" Click="OpenOmrArtifactsButton_Click" IsEnabled="False"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <ProgressRing x:Name="OmrProgressRing" Width="18" Height="18" IsActive="False" Visibility="Collapsed"/>
                            <TextBlock x:Name="OmrProgressText" Text="" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </StackPanel>

                        <TextBlock x:Name="SelectedSheetPathText" Text="未选择图片/PDF文件" TextWrapping="Wrap"/>
                        <TextBlock x:Name="SheetRecognizeSummaryText" Text="OMR 结果: -" TextWrapping="Wrap"/>
                        <TextBlock x:Name="RecognizedMusicXmlPathText" Text="MusicXML: -" TextWrapping="Wrap"/>
                        <TextBlock x:Name="OmrBestReasonText" Text="最优候选: -" TextWrapping="Wrap"/>

                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8"
                                Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
                            <TextBlock x:Name="OmrRuntimeStatusText" Text="运行时状态: -" TextWrapping="Wrap"/>
                        </Border>

                        <TextBlock x:Name="CandidatesTitleText" Text="候选列表" FontWeight="SemiBold"/>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="CandidatesEngineHeaderText" Grid.Column="0" Text="引擎" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CandidatesInputHeaderText" Grid.Column="1" Text="输入版本" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CandidatesNotesHeaderText" Grid.Column="2" Text="音符数" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CandidatesMeasuresHeaderText" Grid.Column="3" Text="小节数" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CandidatesScoreHeaderText" Grid.Column="4" Text="质量分" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CandidatesStatusHeaderText" Grid.Column="5" Text="状态" FontWeight="SemiBold"/>
                        </Grid>

                        <ListView x:Name="OmrCandidatesListView"
                                  Height="220"
                                  SelectionMode="Single"
                                  SelectionChanged="OmrCandidatesListView_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnSpacing="8" Padding="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Engine}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding InputVariant}"/>
                                        <TextBlock Grid.Column="2" Text="{Binding PitchedNotes}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Measures}"/>
                                        <TextBlock Grid.Column="4" Text="{Binding QualityScore}"/>
                                        <TextBlock Grid.Column="5" Text="{Binding Status}" TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <TextBlock x:Name="CandidatesPreviewTitleText" Text="候选预览（MusicXML）" FontWeight="SemiBold"/>
                        <TextBox x:Name="OmrPreviewTextBox"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="180"/>
                    </StackPanel>
                </Border>

                <StackPanel Spacing="10">
                    <Button x:Name="RealtimeNextButton" Content="开始实时识别（下一阶段）" IsEnabled="False"/>
                    <Button x:Name="MicNextButton" Content="麦克风监听转谱（下一阶段）" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
